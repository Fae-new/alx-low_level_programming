#include "main.h"


/**
 * print_most_numbers - Prints numbers from 0 to 9.
 *
 * This function prints the numbers without 2 and 4 from 0 to 9, each sep*arated by a space
* It does not take any parameters.
 *
 * Return: This function does not return a value (void).
 */
void print_most_numbers(void)
{

int i;

for (i = '0'; i <= '9'; i++)
{
if(i == '2' || i == '4')
continue;

_putchar(i);
}
_putchar('\n');
}
